# PDF to Markdown 中文说明

此文件保留为中文入口，主要内容请阅读根目录 [README.md](../README.md)。

项目当前是一个 Zotero 插件，用于将 PDF 附件转换为 Markdown 或 HTML。
它支持 MinerU Token API、轻量模式和本地 MinerU API / Router。

常用命令：

```powershell
npm start
npm run build
npm run lint:check
```

默认本地 Router 地址：

```text
http://127.0.0.1:8002
```
